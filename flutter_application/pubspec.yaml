name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Nuove dipendenze per le API e per salvare il token:
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_appauth: ^6.0.0          # <--- Aggiungi questa
  flutter_secure_storage: ^9.0.0   # <--- Aggiungi questa

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/safeClaimText.png
    